using System.ServiceModel;
using UnityEngine;

public class WebObject : MonoBehaviour {

	// Use this for initialization
	void Start () {
        //ServiceClient client = new IService(new BasicHttpBinding(),
        //    new EndpointAddress("http://localhost:8080/Service.svc"));
        //int n = client.Add(1, 2);
        //Debug.Log(n);
    }
	
	// Update is called once per frame
	void Update () {
		
	}
}
